{% extends 'base.html.twig' %}

{% block title %}Modifier vos informations ♥ Les Amoureux du Chocolat ♥{% endblock %}

{% block body %}
{# <h1 class="mt-5 mb-5 text-center">Modifier vos informations</h1>

<div class="text-center col-6">
    {{ form_start(userForm)}}
    {{ form_row(userForm.firstName)}}
    {{ form_row(userForm.lastName)}}
    <button class="mt-3 mb-3" id="btn-user">Envoyer</button>
    {{ form_end(userForm)}}
</div>
<div class="text-center">
    <p>Pour changer votre addresse veuillez Envoyer un mail a cette adresse : </p>
</div> #}


<div class="row justify-content-center align-content-center d-flex mt-5">
    <div class="col-md-6 col-12 mx-auto justify-content-center align-content-center d-md-flex flex-column">
        <h2 class="mb-5 mt-3 text-center">Modifier vos informations </h2>
        {{ form_start(userForm)}}
        <div class="row mt-4">
            <div class="col text-center">
                {{ form_row(userForm.firstName)}}

                {{ form_row(userForm.lastName)}}

                <button class="mt-3 mb-3" id="btn-user">Envoyer</button>
                {{ form_end(userForm)}}
            </div>
            <div class="text-center mb-5">
                <p>Pour changer votre adresse mail veuillez envoyer, un message a cette adresse :</p>

                <a href="mailto:contact@lesamoureuxduchocolat.com" target="_blank"
                    title="Envoyez un mail à Les amoureux du chocolat"
                    class="mail-contact">contact@lesamoureuxduchocolat.com</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}