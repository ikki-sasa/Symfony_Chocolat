{% extends 'base.html.twig' %}

{% block title %}{{ article.title }} détails Chocoblog ♥ Les Amoureux du Chocolat ♥ {% endblock %}

{% block body %}
<div class="row border border-dark mb-6">
    <div class="col-md-6 ">
        <div class="product">
            <div class="item">
                <img src="{{ asset('img/Blog/' ~ article.featuredImg ) }}" alt="photo principale {{ article.title }}"
                    class="img-fluid mt-5 vh-100 vw-100" id="article-img">
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="product-dtl">
            <div class="product-info mt-5 text-left py-3 px-lg-5">
                <ol class="breadcrumb" id="breadcrumb-article">
                    <ol class="breadcrumb py-3">
                        <li class="breadcrumb-item"><a href=" {{ path('home')}} ">Accueil</a></li>
                        <li class="breadcrumb-item"><a href=" {{ path('article_index')}} ">ChocoBlog article</a>
                        </li>
                        <li class="breadcrumb-item active">{{ article.title }}</li>
                    </ol>
                </ol>
                <h4 class="article-center"> {{ article.event }} </h4>
                <div class="text-left">
                    <h5 class="article-center">{{ article.title}}
                    </h5>
                    <p class="article-center">{{ article.content }}</p>
                </div>
                <a href=" {{ path('comment_new', {'id':article.id}) }} " id="article-center-a">Commenter l'article</a>
                <div class="pt-5">
                    {# <h1 class="text-center mt-5 pb-5">Découvrez ce que nos clients disent de l'événement</h1> #}
                    {% for comment in comments|reverse %}
                </div>
            </div>
        </div>
    </div>
</div>
{# {% if comment.commentStatus == true %}
<div class="container">
    <div class="row mb-2">
        <div class="col-md-6">
            <div class="card flex-md-row mb-4 box-shadow h-md-250">
                <div class="card-body d-flex flex-column align-items-start">
                    <h3 class="mb-0">
                        Commentaire
                    </h3>
                    <div class="mb-1 text-muted">
                        <p>Posté par</p> {{
                        comment.userId.firstName}}
                    </div>

                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card flex-md-row mb-4 box-shadow h-md-250">
                <div class="card-body d-flex flex-column align-items-start">
                    <h3 class="mb-1">{{ article.title }}</h3>
                    <div class="mb-1 text-muted">{{ comment.createdAt|date('d/m/y')}}</div>
                    <p class="card-text mb-5">{{ comment.content}}</p>
                    {% for reponse in reponses%}
                    {% if reponse.fkcomment.id == comment.id %}
                    {{ reponse.answer }}
                    {% endif %}
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %} #}
²
<section class="pt-3 pb-3" style="background-image: url()">
    <div class="container">
        <h1 class="text-center mt-2 pb-4">Découvrez ce que nos clients disent de l'événement</h1>
        <div class="row align-items-center justify-content-center border border-dark">
            <div class="col-12 col-md-1 mt-4 mt-md-0  me-auto me-md-0">
                <img alt="utilisateur les amoureux du chocolat" class="img-fluid rounded-circle mb-3 mb-md-1"
                    src=" {{ asset('img/Blog/user.png')  }} ">
            </div>
            <div class="col-12 col-md-7">
                <h4>comment title</h4>
                <div class="mb-1 text-muted text-end">{{ comment.createdAt|date('d/m/y')}}</div>
                <p class="text-h3 pt-2">
                    "Vivamus sagittis lacus vel augue laoreet rutrum faucibus dolor
                    auctor. Duis mollis, est non commodo luctus. Vivamus sagittis
                    lacus vel augue laoreet, rutrum faucibus dolor auctor."
                </p>
                <p class="text-h3 mt-2"><strong>Person Name</strong>&nbsp;
                    &nbsp;&nbsp;<em>Co-founder at Company</em></p>
            </div>

        </div>
    </div>
</section>

{% endfor %}
{% include "shared/_scrolltop.html.twig" %}
{% endblock %}