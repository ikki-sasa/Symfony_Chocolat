{% if pageCount > 1 %}
{% if previous is defined %}
<a rel="prev" href="{{ path(route, query|merge({(pageParameterName): previous})) }}" class="btn btn-sm  mx-3"><i
        class="fas fa-angle-double-left"></i>
</a>
{% else %}
<a class="btn btn-sm btn-secondary mx-3" disabled>
</a>
{% endif %}
Produits {{ firstItemNumber }} à {{ lastItemNumber }} (sur {{totalCount}})
{% if next is defined %}
<a rel="next" href="{{ path(route, query|merge({(pageParameterName): next})) }}" class="btn btn-sm mx-3"><i
        class="fas fa-angle-double-right"></i></a>
{% else %}
<a class="btn btn-sm btn-secondary mx-3" disabled></a>
{% endif %}
{% endif %}