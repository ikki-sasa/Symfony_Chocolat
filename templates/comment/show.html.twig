{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
<h1>Comment</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ comment.id }}</td>
        </tr>
        <tr>
            <th>Content</th>
            <td>{{ comment.content }}</td>
        </tr>
    </tbody>
</table>

<a href="{{ path('comment_index') }}">back to list</a>

<a href="{{ path('comment_edit', {'id': comment.id}) }}">edit</a>

{{ include('comment/_delete_form.html.twig') }}
{% endblock %}

{# {% extends 'base.html.twig' %}

{% block title %}Laisser un commentaire ♥ Les Amoureux du Chocolat ♥{% endblock %}

{% block body %}
{# récuperer l'image de l'article pour le mettre en header du commentaire #}
{# <section class="pt-5 pb-1 mt-0 align-items-center d-flex bg-dark"
    style="min-height: 130vh; max-width: 500vw; background-size: cover; background-image: url(../img/Blog/merci.jpg);">
    <div class="container-fluid">
        <div class="row  justify-content-center align-items-center d-flex-row text-center h-100">
            <div class="col-12 col-md-8  h-50 ">
                <div class="icon-wrap text-primary d-flex justify-content-center my-3">
                </div>
                <h1 class="text-white mb-2 mt-2">Laisser nous un commentaire
                </h1>
            </div>
        </div>
    </div>
</section>
<h1 class="mt-5 mb-5"></h1> #}
{# récuperer l'image de l'article pour le mettre en header du commentaire #}


{#
{{ include('comment/_form.html.twig') }}

{% endblock %} #}